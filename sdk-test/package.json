{
  "name": "paqett-sdk-test",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run --reporter=verbose --sequence.sequential",
    "test:build": "cd firmware && pio run",
    "test:flash": "cd firmware && pio run -t upload",
    "test:watch": "vitest --reporter=verbose --sequence.sequential"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["@serialport/bindings-cpp", "esbuild"]
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "mqtt": "^5.10.0",
    "serialport": "^12.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
